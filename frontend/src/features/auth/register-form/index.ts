/** Registration form Client Component. */
export { RegisterForm } from './ui/RegisterForm'
/** Server Action — registers a new account and auto-logs in on success. */
export { registerAction } from './api/register.action'
/** Result type for {@link registerAction}. */
export type { RegisterActionResult } from './api/register.action'
